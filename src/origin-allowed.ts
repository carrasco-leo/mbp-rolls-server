//
// origin-allowed.ts — mbp-rolls-server
// ~/src
//

/**
 * Check if we allow a client based on the url it came from.
 *
 * @return true if the origin is allowed
 */
export function isOriginAllowed(origin: string): boolean {
	return true;
}
